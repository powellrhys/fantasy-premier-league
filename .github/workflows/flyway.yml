name: Flyway Migration

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches:
      - main

concurrency:
  group: flyway-migration
  cancel-in-progress: false

jobs:
  migrate:
    runs-on: ubuntu-latest

    env:
      RESOURCE_GROUP: powellrhys-sql-server-rg
      SQL_SERVER: powellrhys-sqlserver
      FIREWALL_RULE_NAME: github-actions-${{ github.run_id }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Login to Azure using the service principal JSON
      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      # Get runner's public IP address
      - name: Get GitHub Runner Public IP
        id: ip
        run: |
          IP=$(curl -s https://api.ipify.org)
          echo "Runner IP: $IP"
          echo "ip=$IP" >> $GITHUB_OUTPUT

      # Add temporary firewall rule for this IP
      - name: Add Azure SQL Firewall Rule
        run: |
          az sql server firewall-rule create \
            --resource-group $RESOURCE_GROUP \
            --server $SQL_SERVER \
            --name $FIREWALL_RULE_NAME \
            --start-ip-address ${{ steps.ip.outputs.ip }} \
            --end-ip-address ${{ steps.ip.outputs.ip }}

      # Run Flyway migrations
      - name: Run Flyway migrations
        uses: docker://flyway/flyway:latest
        with:
          args: >
            -url=${{ secrets.SQL_SERVER_JDBC_URL }}
            -user=${{ secrets.SQL_SERVER_USERNAME }}
            -password=${{ secrets.SQL_SERVER_PASSWORD }}
            -locations=filesystem:./database/flyway
            -connectRetries=10
            migrate

      # Always remove the firewall rule (even if migration fails)
      - name: Remove Azure SQL Firewall Rule
        if: always()
        run: |
          az sql server firewall-rule delete \
            --resource-group $RESOURCE_GROUP \
            --server $SQL_SERVER \
            --name $FIREWALL_RULE_NAME
